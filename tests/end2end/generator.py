import Geometry3D as geometry
import sys
import argparse

def create_triangles()

def main ():
    parser = argparse.ArgumentParser(description='file name and number of triangles')
    parser.add_argument('file_name', type=str, help='generating file name')
    parser.add_argument('triangles_number', type=int, help='number of generating triangles')